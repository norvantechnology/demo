# Leave unset to use same-origin /api (Vite proxy locally, Vercel rewrites in prod).
# Only set this if the browser should call Render directly (then update CORS + cookies).
# VITE_API_URL=https://a2z-printing-api.onrender.com/api
